package com.lyntra.lyntraclans.gui;

/** Implementado por qualquer frame de bigorna que precisa capturar o texto digitado (ver AnvilInputListener). */
public interface AnvilTextCapture {
    void captureRenameText(String text);
}
